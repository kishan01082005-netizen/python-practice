import math
sum=0
arr=[1,2,5,9]
threshold=6
low=min(arr)
high=max(arr)
while low<=high:
    mid=(low+high)//2
    sum=0
    for i in arr:
        sum+=math.ceil(i/mid)
    if sum<=threshold:
        ans=mid
        high=mid-1
    else:
        low=mid+1
print(ans)
        